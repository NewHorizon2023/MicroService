spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservice?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update

mybatis-plus:
  global-config:
    db-config:
      id-type: auto

logging:
  level:
    com.itheima: debug
  pattern:
    dateformat: HH:mm:ss
